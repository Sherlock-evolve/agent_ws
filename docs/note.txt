# 创建环境
mkdir agent_ws
cd agent_ws
python -m venv .venv

# 启动环境
source .venv/bin/activate

# 安装依赖
pip install langchain python-dotenv langchain-openai

# 创建项目文件
touch .env main.py .gitignore

# 运行
unset ALL_PROXY all_proxy
python main.py

